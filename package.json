{
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "build": "cd frontend && npm install --only=dev && npm install && npm run build && cp -r build ../backend/"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  },
  "dependencies": {
    "config": "^3.3.1",
    "path": "^0.12.7"
  }
}
